#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <bits/stdc++.h>
using namespace std;

bool isPositiveInt(const string &s);
void replace(string &s,const string &a,const string &b);
string getTime();

#endif
